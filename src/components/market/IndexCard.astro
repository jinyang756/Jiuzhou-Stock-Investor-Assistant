---
const { title, value, change, isRise } = Astro.props;
---
<div class="bg-white/5 backdrop-blur-sm p-6 rounded-xl transition-all duration-300 hover:scale-105 hover:shadow-blue-500/30">
  <h3 class="text-lg font-medium mb-2">{title}</h3>
  <p class={`text-3xl font-bold mb-1 ${isRise ? 'text-green-400' : 'text-red-400'}`}>{value}</p>
  <p class={`text-sm ${isRise ? 'text-green-400' : 'text-red-400'}`}>{change}</p>
</div>